[project]
name = "diffusion-models-from-scratch"
version = "0.1.0"
description = "Diffusion models implemented from scratch"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "torch",
    "torchvision",
    "einops",
    "marimo",
    "scipy",
    "tqdm",
    "denoising-diffusion-pytorch>=2.2.6",
]

[tool.uv]
package = false
